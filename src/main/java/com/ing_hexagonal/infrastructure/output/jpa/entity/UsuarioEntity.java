package com.ing_hexagonal.infrastructure.output.jpa.entity;

/**
 * Entidad JPA que representa la tabla de usuarios en la base de datos.
 /*
 * Su función es mapear la información persistente del usuario
 * al modelo relacional utilizado por el sistema.
 * Aunque contiene datos equivalentes al modelo de dominio,
 * esta clase pertenece a infraestructura y está acoplada a JPA.
 */
public class UsuarioEntity {
}
